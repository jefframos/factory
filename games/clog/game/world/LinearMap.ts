// Re-exports from LinearConfig.ts — kept for backward compatibility with
// PlayerHud and LinearMinimap which import from this module.
export {
    getLinearRoomConfig as getLinearConfig,
    KING_ROOM_INDEX,
    linearRoomLabel,
} from './LinearConfig';
export type { LinearRoomConfig } from './LinearConfig';
